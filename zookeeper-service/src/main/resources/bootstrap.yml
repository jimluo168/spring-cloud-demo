server:
  port: 8080

spring:
  application:
    name: zookeeper-service
  cloud:
    zookeeper:
      connect-string: 127.0.0.1:2181
      discovery:
        root: /develop
        enabled: true
        register: true